export enum PageEnum {
  // basic login path
  BASE_LOGIN = '/login',
  // basic home path
  BASE_HOME = '/home',
  // 404 page path
  NOT_FOUND = '/not-found',
  // 403 page path
  NO_PERMISSION = '/no-permission',
}
